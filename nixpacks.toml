[phases.setup]
aptPkgs = ["libsm6", "libxext6", "libxrender-dev", "libgomp1", "libglib2.0-0", "libgl1"]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "./start.sh"
